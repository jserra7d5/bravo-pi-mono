{
  "name": "@bravo/goals",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "description": "Pi-first durable goal workspaces, task receipts, and Judge verification.",
  "keywords": ["pi-package"],
  "bin": {
    "bravo-goals": "./dist/src/cli.js"
  },
  "scripts": {
    "prebuild": "npm run build --workspace @bravo/render-clock",
    "build": "tsc -p tsconfig.json",
    "precheck": "npm run build --workspace @bravo/render-clock",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bravo/render-clock": "file:../render-clock",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": ["./extensions/pi"]
  }
}
