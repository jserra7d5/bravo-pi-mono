{
  "name": "@bravo/browser-workspace",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "bin": { "bravo-browser-workspace": "dist/src/cli.js" },
  "engines": { "node": ">=22 <23" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "smoke:live": "npm run build && node dist/src/live-smoke.js"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "playwright": "1.55.0",
    "typescript": "5.9.2"
  }
}
