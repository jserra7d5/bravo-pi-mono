{
  "name": "@bravo/loom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLI-first recursive work graph for durable agent context.",
  "keywords": ["pi-package"],
  "bin": { "loom": "./dist/src/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {},
  "devDependencies": { "@types/node": "latest", "typescript": "latest" }
}
